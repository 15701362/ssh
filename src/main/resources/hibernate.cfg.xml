<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>

    <session-factory>
        <!-- 数据库连接配置 -->
       <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/my_hibernate_demo_db?serverTimezone=UTC</property>
        <property name="connection.username">root</property>
        <property name="connection.password">admin</property>

        <!-- 数据库连接池的大小 -->
        <property name="connection.pool_size">5</property>
        <!-- 每次从数据库中取出并放到JDBC的Statement中的记录条数。Fetch Size设的越大，读数据库的次数越少，速度越快，Fetch Size越小，读数据库的次数越多，速度越慢-->
        <property name="jdbc.fetch_size">50</property>
        <!--批量插入,删除和更新时每次操作的记录数。Batch Size越大，批量操作的向数据库发送Sql的次数越少，速度就越快，同样耗用内存就越大-->
        <property name="jdbc.batch_size">20</property>

        <!-- SQL 方言 -->
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Enable Hibernate's automatic session context management -->
        <!-- 将getCurrentSession()返回的session绑定到当前运行线程中 -->
        <property name="current_session_context_class">thread</property>

        <!-- Disable the second-level cache -->
        <!--<property name="cache.provider_class">org.hibernate.cache.internal.NoCacheProvider</property>-->

        <!-- 输出sql语句 -->
        <property name="show_sql">true</property>

        <!-- 开启二级缓存 ehcache -->
        <property name="hibernate.cache.use_second_level_cache">true</property>
        <!-- 开启查询的二级缓存  如果不需要就不设置-->
        <property name="hibernate.cache.use_query_cache">true</property>
        <!-- Hibernate4.0以上设置factory -->
        <property name="hibernate.cache.region.factory_class">org.hibernate.cache.ehcache.EhCacheRegionFactory</property>
        <!-- 二级缓存 ehcache的配置文件位置 -->
        <property name="hibernate.cache.provider_configuration_file_resource_path">ehcache.xml</property>

        <!-- 映射实体 -->
       <mapping class="com.sunliang.hibernate.demo.entity.Department" />
        <mapping class="com.sunliang.hibernate.demo.entity.Employee" />
        <mapping class="com.sunliang.hibernate.demo.entity.SalaryGrade" />
        <mapping class="com.sunliang.hibernate.demo.entity.Timekeeper" />

    </session-factory>

</hibernate-configuration>